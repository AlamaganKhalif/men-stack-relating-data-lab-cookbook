<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>
  <nav>
    <a href="/">Home</a> |
    <a href="/recipes">Recipes</a> |
    <a href="/ingredients">Ingredients</a> |
    <% if (user) { %>
      Welcome, <%= user.username %> | <a href="/auth/sign-out">Sign out</a>
    <% } else { %>
      <a href="/auth/sign-in">Sign in</a> |
      <a href="/auth/sign-up">Sign up</a>
    <% } %>
  </nav>
  <hr>
  
  <% if (user) { %>
    <h1>Welcome to the app, <%= user.username %>!</h1>
    <p>
      <a href="/auth/sign-out">Sign out</a>
    </p>
  <% } else { %>
    <h1>Welcome to the app, guest.</h1>
    <p>
      <a href="/auth/sign-up">Sign up</a> or
      <a href="/auth/sign-in">Sign in</a>.
    </p>
  <% } %>
  <p><a href="/vip-lounge">Get into the VIP Users Only lounge!</a></p>
</body>
</html>
